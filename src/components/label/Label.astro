---
const { class: className, ...props } = Astro.props;

const baseClasses = "block text-sm font-medium text-gray-700 mb-1";

const labelClasses = className || baseClasses;
---

<label class={labelClasses} {...props}>
  <slot />
</label>
